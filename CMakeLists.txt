CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(SWEET)

AUX_SOURCE_DIRECTORY(src DIR_SOURCE)

ADD_EXECUTABLE(sweet ${DIR_SOURCE})

MESSAGE(STATUS ${PROJECT_SOURCE_DIR})#print current dir path

MESSAGE(STATUS ${PROJECT_BINARY_DIR})

MESSAGE(STATUS ${DIR_SOURCE})
